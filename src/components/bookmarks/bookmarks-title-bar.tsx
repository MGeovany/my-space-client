import React from 'react'
import { TitleBar } from '../list-detail/TitleBar'

export const BookmarksTitleBar = ({ scrollContainerRef }: any) => {
  return <TitleBar scrollContainerRef={scrollContainerRef} title="Bookmarks" />
}
